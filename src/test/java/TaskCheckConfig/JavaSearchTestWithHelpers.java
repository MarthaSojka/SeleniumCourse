package TaskCheckConfig;

import com.course.selenium.Helper;
import org.junit.After;
import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
/*
public class JavaSearchTestWithHelpers {

        WebDriver driver;

        private static final By BUTTON_NARZEDZIA = By.xpath("..*contains(text(), 'Narzędzia')]");

        @Test
        public void googleSearchConfig_test() {

            // Object WebDriver is a new instance of ChromeDriver()
            driver = new ChromeDriver();
            // Maximise window
            driver.manage().window().maximize();
            // Open page
            driver.get("http://www.google.com");
            // Close cookie window
            WebElement elementCookiesWindow = driver.findElement(By.xpath("//button/*[contains(text(),'Zaakceptuj')]"));
            elementCookiesWindow.click();

          //  Helper.fillInput("Narzędzia", "locator");

            // Add assertion
  //          Assert.assertEquals("Coderslab - Szukaj w Google", driver.getTitle());
            // Close driver and browser - prevent leak of memory!!!
            driver.quit();
        }

        @After
        public void tearDown() {
            driver.quit();
        }
    }
}
*/